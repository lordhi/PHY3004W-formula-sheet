\begin{equation}
    s = (p_{A} + p_{B})^{2}
\end{equation}

\begin{center}
    \feynmandiagram[vertical=b to c] {
        a [particle=$X^{\pm}$] -- [fermion] b -- [photon] c [particle=$\gamma$],
        b  -- [fermion] d [particle=$X^{\pm}$],
    };
\end{center}

\subsection{Weak Force}

\begin{center}
    \feynmandiagram[vertical=b to c] {
        a [particle=$f$] -- [fermion] b -- [boson] c [particle=$Z$],
        b  -- [fermion] d [particle=$f$],
    };

    \feynmandiagram[vertical=b to c] {
        a [particle=$\ell^{-}$] -- [fermion] b -- [boson] c [particle=$W^{-}$],
        b  -- [fermion] d [particle=$\nu_{\ell}$],
    };

    \feynmandiagram[vertical=b to c] {
        a [particle=$q^{-\frac{1}{3}}$] -- [fermion] b -- [boson] c [particle=$W^{-}$],
        b  -- [fermion] d [particle=$q^{+\frac{2}{3}}$],
    };
\end{center}

\subsection{Strong Force}

\begin{center}
    \feynmandiagram[vertical=b to c] {
        a [particle=$q$] -- [fermion] b -- [gluon] c [particle=$g$],
        b  -- [fermion] d [particle=$q$],
    };

    \feynmandiagram[vertical=b to c] {
        a [particle=$g$] -- [gluon] b -- [gluon] c [particle=$g$],
        b  -- [gluon] d [particle=$g$],
    };


    \feynmandiagram[vertical=a to b] {
        a [particle=$g$] -- [gluon] c -- [gluon] d [particle=$g$],
        b [particle=$g$]  -- [gluon] c  -- [gluon] e [particle=$g$],
    };
\end{center}

\begin{equation}
    \d{N_{\text{scat}}}{t} = \sigma \mathcal{L}
\end{equation}

\begin{equation}
    \Gamma = \frac{|\vec{p}|}{8\pi m^{2}_{A}} |\mathcal{M}|^{2} \comment{two-body decay}
\end{equation}

\begin{equation}
    \d{\sigma}{\Omega} = \left( \frac{1}{8\pi} \right)^{2} \frac{|\mathcal{M}|^{2}}{(E_{A} + E_{B})^{2}} \frac{|\vec{p}_{f}|}{|\vec{p}_{i}|} \comment{$2 \rightarrow 2 scatter$}
\end{equation}
